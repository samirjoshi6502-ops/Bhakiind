import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Bhakti Enterprise | Ice Cream Machine Manufacturer",
  description:
    "Bhakti Enterprise manufactures and exports high-performance Kulfi machines, ice cream machines, and Khava making equipment for commercial and industrial use.",
};

export default function RootLayout({ children }: Readonly<{ children: React.ReactNode }>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full bg-[#071d2d] text-slate-50">{children}</body>
    </html>
  );
}
